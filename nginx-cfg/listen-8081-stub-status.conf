
server {
    # Listen only on localhost
    listen       127.0.0.1:8081 default_server;
    listen       [::1]:8081 default_server;
    server_name  _;

    access_log off;

    location = / {
        stub_status;
    }

}

